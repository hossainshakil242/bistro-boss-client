import { Helmet } from "react-helmet";

const Menu = () => {
    return (
        <div>
            <Helmet>
                <title>Bistro Boss | Menu</title>
            </Helmet>
            Hello Menu
        </div>
    );
};

export default Menu;